# discriminated-union

<a id="overview"></a>

## Overview

Use `md.discriminatedUnion(discriminator, variants)` for tagged objects with `md.literal(...)` discriminator values.

## Method Index

### Type-specific methods

- [discriminatedUnion](/api/types/discriminated-union/discriminated-union)

### Shared auxiliaries

- No shared auxiliaries linked.












