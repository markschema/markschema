# date

<a id="overview"></a>

## Overview

Use `md.date()` for date parsing. Output mode defaults to `Date`; use `{ as: 'string' }` for ISO string output.

## Method Index

### Type-specific methods

- [date](/api/types/date/date)
- [as: 'string'](/api/types/date/as-string)

### Shared auxiliaries

- [transform](/api/aux/transform)












