# mdshape Playground

Standalone Next.js + React + Tailwind playground for `mdshape`.

## Local development

```bash
npm run --workspace @markschema/playground dev
```

App URL: `http://localhost:3001`

## Build

```bash
npm run --workspace @markschema/playground build
```
